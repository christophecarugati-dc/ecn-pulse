# Python
__pycache__/
*.pyc
.venv/
venv/

# Local scraper output only (root-level). The Action's data/ecn_pulse.json is
# tracked and must NOT be ignored — hence the leading slash to anchor to root.
/ecn_pulse.json
/test_*.json

# OS
.DS_Store
